plugins {
    id 'java'
}

group 'wmeds'
version '1.0'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

jar {
    manifest {
        attributes 'Main-Class': 'main.Main'
    }
    archiveBaseName.set("ProducaoGames")
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
}
